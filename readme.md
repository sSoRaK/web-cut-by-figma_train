<!-- Upload source code on git hub by Terminal -->
<!--
    git init -> khởi tạo git ở folder hiện tại
    git remote add origin https://github.com/sSoRaK/[yourRepositoriesName.git]  -> kết nối file của mình lên git hub
    git add . -> thêm (.) toàn bộ file mình chỉnh sữa
    git commit -m "yourMessage" thêm tin nhắn mô tả file vừa commit lên gitHub
    git push origin master -> đẩy src code lên Git Hub
 -->
<!--
    kiểm tra file vừa thêm hoặc chỉnh sửa:

    git status -> kiểm tra tình trạng file vừa upload có gì thêm hay chỉnh sửa
=> sau khi check status lặp lại bước sau để đẩy code lên Git Hub
    git add . -> thêm (.) toàn bộ file mình chỉnh sữa
    git commit -m "yourMessage" thêm tin nhắn mô tả file vừa commit lên gitHub
    git push origin master -> đẩy src code lên Git Hub
 -->
